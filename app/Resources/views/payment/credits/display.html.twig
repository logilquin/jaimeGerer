{% block body %}
    <div class="modal-content">
        <div class="modal-header">
            <a class="close" data-dismiss="modal">x</a>
             <h3>Crédit disponible</h3>
        </div>
        <div class="modal-body">
            <h3 class="center">Votre crédit disponible: {{ app.user.company.credits|number_format(2, ',') }} €</h3><br />
            <h4>A quoi sert le crédit ?</h4>
            <p>
                Impression, mise sous pli, affranchissement, dépose à la Poste ? Tout ça est derrière vous&nbsp;!<br />
                En achetant du crédit, vous pouvez envoyer vos factures ou relances par courrier en trois clics,
                directement depuis J'aime Gérer. L'envoi est effectué depuis Maileva, filliale de la Poste.
            </p>
            <h4>Coût du service :</h4>
            <ul class="credit_prices">
                {% for label, price in prices %}
                    <li>{{ label }}: {{ price|number_format(2, ',') }} € HT / envoi</li>
                {% endfor %}
            </ul>
        </div>
        <div class="modal-footer center">
            <a class="btn-success btn"  href="{{ path('payment_credits_choose') }}">
                <span class="glyphicon glyphicon-shopping-cart"> </span>
                Acheter du crédit
            </a>
        </div>
    </div>
{% endblock %}