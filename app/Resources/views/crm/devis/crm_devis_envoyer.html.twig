<div class="modal-header">
	<a class="close" data-dismiss="modal">×</a>
	<h3>Envoyer un devis par email</h3>
</div>
<div class="modal-body">

	<div class="center">
		<p>Le devis <strong>"{{ devis.num}} - {{ devis.objet }}"</strong> sera envoyé à <strong>{{ devis.contact }}</strong> ({{ devis.contact.email }}).</p>
 			{{ form(form, {'action': path('crm_devis_envoyer', {'id' : devis.id })} ) }}
		<p id="loading-message" class="hidden">
		<i>Votre message est en cours d'envoi. Pour patienter, voici un petit chat qui danse :</i><br />
		<img src="{{ asset('images/assets/cat_loader.gif') }}" alt="Patientez..." />
	</p>
	</div>
</div>


{{ tinymce_init() }}

<script type="text/javascript">
$(document).ready( function () {

	$("form").submit(function(){
		$("input").attr('readonly', true);
		$('#form_submit').addClass("hidden");
		$("#loading-message").removeClass("hidden");
	});
});
</script>
